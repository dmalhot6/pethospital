apiVersion: v1
kind: Secret
metadata:
  name: ecr-registry-secret
  namespace: pethospital-dev
type: kubernetes.io/dockerconfigjson
data:
  # This will be populated by the GitHub Actions workflow
  .dockerconfigjson: ${ECR_REGISTRY_SECRET}
